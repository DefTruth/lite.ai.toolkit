# project(litencnn) 这句会影响 PROJECT_xxx_DIR 如果设置，则与当前工程所在目录相同；否则与父工程相同
message("=================================================================================")
message("Belows are debug information ...")
message("=================================================================================")
message("THIRDPARTY_DIR in ncnn is ${THIRDPARTY_DIR}")
message("PROJECT_SOURCE_DIR in ncnn is ${PROJECT_SOURCE_DIR}")
message("PROJECT_SOURCE_DIR in ncnn is ${PROJECT_SOURCE_DIR}")
message("CMAKE_SOURCE_DIR in ncnn is ${CMAKE_SOURCE_DIR}")
message("CMAKE_BINARY_DIR in ncnn is ${CMAKE_BINARY_DIR}")
message("CMAKE_CURRENT_BINARY_DIR in ncnn is ${CMAKE_CURRENT_BINARY_DIR}")
message("CMAKE_CURRENT_SOURCE_DIR in ncnn is ${CMAKE_CURRENT_SOURCE_DIR}")
message("LIBRARY_OUTPUT_PATH in ncnn is ${LIBRARY_OUTPUT_PATH}")
message("EXECUTABLE_OUTPUT_PATH in ncnn is ${EXECUTABLE_OUTPUT_PATH}")
message("=================================================================================")
